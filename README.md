# 🌱 Plant Management System

Система управления растениями с использованием Laravel 10 + Vue.js 3 + Vuex 4

---

## 📋 О приложении

**Plant Management System** - веб-приложение для управления растениями, их типами и средствами защиты.

### ✨ Функционал:

- ✅ **CRUD операции** - создание, просмотр, редактирование, удаление растений
- ✅ **Типы растений** - фруктовые, хвойные, декоративные растения
- ✅ **Средства защиты** - привязка нескольких препаратов к растению с указанием дозы (ml/l)
- ✅ **SPA интерфейс** - Vue.js 3, без перезагрузки страницы
- ✅ **Vuex Store** - централизованное управление состоянием
- ✅ **REST API** - полноценный API на Laravel
- ✅ **Уведомления** - всплывающие сообщения об успехе/ошибке
- ✅ **Модальные окна** - красивые диалоги подтверждения удаления
- ✅ **Валидация** - серверная и клиентская валидация данных

---

## 🛠 Технологии

### Backend:
- PHP 8.1+
- Laravel 10
- MySQL 8 (Docker)
- Repository Pattern
- Service Layer
- SOLID принципы

### Frontend:
- Vue.js 3 (Composition API)
- Vuex 4 (State Management)
- Vue Router 4
- Vite (Build Tool)
- Axios (HTTP Client)

### DevOps:
- Docker & Docker Compose
- Laravel Vite Plugin

---

## 📊 Структура данных

### Основные сущности:

**Plant (Растение)**
- id - уникальный идентификатор
- title - название
- age - возраст (лет)
- type_id - тип растения (FK)

**Plant Type (Тип растения)**
- id - уникальный идентификатор
- title - название типа

**Plant Protection Product (Средство защиты)**
- id - уникальный идентификатор
- title - название препарата

**Pivot таблица (plant_protection_product)**
- plant_id - ID растения
- product_id - ID препарата
- dose - доза применения (decimal)

### Связи:
- Plant → Plant Type (Many-to-One)
- Plant ↔ Protection Products (Many-to-Many с дополнительным полем dose)

---

## Старт

### Требования:
- PHP 8.1 или выше
- Composer
- Node.js 16+ & npm
- Docker & Docker Compose

### Установка:

#### Шаг 1: Запустить MySQL в Docker
```bash
docker-compose up -d
```

#### Шаг 2: Установить PHP зависимости
```bash
composer install
```

#### Шаг 3: Установить JavaScript зависимости
```bash
npm install
```

#### Шаг 4: Настроить окружение
```bash
cp .env.example .env
php artisan key:generate
```

#### Шаг 5: Создать и заполнить базу данных
```bash
php artisan migrate --seed
```

#### Шаг 6: Запустить приложение

Откройте **2 терминала**:

**Терминал 1 - Frontend (Vite):**
```bash
npm run dev
```
Vite запустится на http://localhost:3001

**Терминал 2 - Backend (Laravel):**
```bash
php artisan serve
```
Laravel запустится на http://localhost:8000

#### Шаг 8: Открыть приложение
```
http://localhost:8000
```

---

## 🔧 Альтернативный запуск на другом порту

Если порт 8000 занят:

```bash
# вручную указать порт
php artisan serve --port=8080
```

Для Vite:
```bash
# Указать порт в package.json или командой
npm run dev -- --port=3001
```

---

## 📁 Структура проекта

```
plant-management/
├── app/
│   ├── Http/
│   │   ├── Controllers/Api/
│   │   │   ├── PlantController.php
│   │   │   ├── PlantTypeController.php
│   │   │   └── PlantProtectionProductController.php
│   │   ├── Requests/
│   │   │   ├── StorePlantRequest.php
│   │   │   └── UpdatePlantRequest.php
│   │   └── Resources/
│   │       └── PlantResource.php
│   ├── Models/
│   │   ├── Plant.php
│   │   ├── PlantType.php
│   │   └── PlantProtectionProduct.php
│   ├── Repositories/
│   │   ├── PlantRepository.php
│   │   └── PlantRepositoryInterface.php
│   └── Services/
│       └── PlantService.php
│
├── database/
│   ├── migrations/
│   │   ├── create_plant_types_table.php
│   │   ├── create_plants_table.php
│   │   ├── create_plant_protection_products_table.php
│   │   └── create_plant_protection_product_table.php (pivot)
│   └── seeders/
│       ├── PlantTypeSeeder.php
│       └── PlantProtectionProductSeeder.php
│
├── resources/
│   ├── js/
│   │   ├── components/
│   │   │   ├── Notification.vue
│   │   │   └── ConfirmDialog.vue
│   │   ├── views/
│   │   │   ├── PlantList.vue
│   │   │   ├── PlantForm.vue
│   │   │   └── PlantDetail.vue
│   │   ├── store/
│   │   │   ├── index.js
│   │   │   └── modules/
│   │   │       ├── plants.js
│   │   │       ├── plantTypes.js
│   │   │       └── protectionProducts.js
│   │   ├── services/
│   │   │   └── plantService.js
│   │   ├── router.js
│   │   └── app.js
│   └── views/
│       └── app.blade.php
│
├── routes/
│   ├── api.php
│   └── web.php
│
├── docker-compose.yml
├── package.json
├── vite.config.js
└── README.md
```

---

## 🌐 API Endpoints

### Plants (Растения)
```
GET    /api/v1/plants              - Получить список всех растений
GET    /api/v1/plants/{id}         - Получить одно растение
POST   /api/v1/plants              - Создать растение
PUT    /api/v1/plants/{id}         - Обновить растение
DELETE /api/v1/plants/{id}         - Удалить растение
```

### Plant Types (Типы растений)
```
GET    /api/v1/plant-types         - Получить список типов
```

### Protection Products (Средства защиты)
```
GET    /api/v1/protection-products - Получить список препаратов
```

---

## 🎨 Особенности реализации

### Backend:

#### Repository Pattern
```php
PlantController → PlantService → PlantRepository → Plant Model
```

Разделение ответственности:
- **Controller** - обработка HTTP запросов
- **Service** - бизнес-логика
- **Repository** - работа с базой данных
- **Model** - Eloquent модель

#### Валидация
- **StorePlantRequest** - валидация при создании
- **UpdatePlantRequest** - валидация при обновлении

#### API Resources
- **PlantResource** - трансформация данных для API ответа

### Frontend:

#### Vuex Store Structure
```
store/
├── index.js           # Главный store + notifications
└── modules/
    ├── plants.js      # Управление растениями
    ├── plantTypes.js  # Типы растений
    └── protectionProducts.js  # Препараты
```

#### Компоненты:
- **PlantList.vue** - список растений с возможностью просмотра, редактирования, удаления
- **PlantForm.vue** - форма создания/редактирования с выбором препаратов и доз
- **PlantDetail.vue** - детальный просмотр одного растения
- **Notification.vue** - всплывающие уведомления
- **ConfirmDialog.vue** - модальное окно подтверждения

#### Маршруты:
```
/                    - Список растений (PlantList)
/plants/create       - Создание растения (PlantForm)
/plants/:id          - Просмотр растения (PlantDetail)
/plants/:id/edit     - Редактирование (PlantForm)
```

---

## 🗄️ База данных (Docker)

### Настройки по умолчанию:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=plant_mysql
DB_USERNAME=user
DB_PASSWORD=password
```

### Управление Docker контейнером:

```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Просмотр логов
docker-compose logs -f

# Перезапустить
docker-compose restart

# Удалить с данными
docker-compose down -v
```

---

## 🧪 Тестовые данные

После выполнения `php artisan migrate --seed` будут созданы:

### Типы растений:
- fruit plants (фруктовые растения)
- coniferous plants (хвойные растения)
- ornamental plants (декоративные растения)

### Средства защиты:
- Actara
- Ridomil Gold
- Topaz
- Konfidor Maxi

---

## 🐛 Troubleshooting

### Порт 8000 занят:
```bash
php artisan serve --port=8080
```

### Docker не запускается:
```bash
# Проверить статус
sudo systemctl status docker

# Запустить Docker
sudo systemctl start docker

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### Ошибки миграций:
```bash
# Пересоздать базу
php artisan migrate:fresh --seed
```

### Проблемы с npm:
```bash
# Очистить кеш
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Vite не подключается:
```bash
# Проверить запущен ли Vite
npm run dev

# Проверить файл public/hot
cat public/hot
```

---

## 📝 Примечания

### Структура запроса при создании растения:

```json
{
  "title": "Rose",
  "age": 1,
  "type_id": 1,
  "protection_products": [
    {
      "id": 1,
      "dose": 15.5
    },
    {
      "id": 3,
      "dose": 0.20
    }
  ]
}
```

### Структура ответа API:

```json
{
  "data": {
    "id": 1,
    "title": "Rose",
    "age": 1,
    "type": {
      "id": 1,
      "title": "fruit plants"
    },
    "protection_products": [
      {
        "id": 1,
        "title": "Topaz",
        "pivot": {
          "dose": "15.50"
        }
      }
    ]
  }
}
```

---

## 📄 Лицензия

Этот проект создан в образовательных целях.

---

## 👤 Автор

Разработано как тестовое задание для демонстрации навыков:
- Laravel 10 (Backend API)
- Vue.js 3 + Vuex (Frontend SPA)
- Repository Pattern & Service Layer
- REST API
- Docker
- SOLID принципы

---

## 🎉 Готово!

Приложение готово к использованию. Откройте http://localhost:8000 и начните управлять растениями!

Если возникли вопросы - проверьте раздел **Troubleshooting** выше.
